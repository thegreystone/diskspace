# Working / personal notes — not for the repo
DESIGN.md

# Build output
target/
build/
out/
*.class
dependency-reduced-pom.xml

# Profiling output
*.jfr

# Inno Setup output (installer build artifacts + staged binary)
installer/Output/
installer/diskspace.exe

# GraalVM / Gluon native-image
target/**/*.exe
target/**/*.so
target/**/*.dylib
target/**/*.dll
# Note: icon assets under src/<platform>/assets/ (icon.ico, icon.icns, icon.png) are
# tracked deliberately; they're checked-in bundle assets, not build output.
gluonfx/
native-image-agent/
META-INF/native-image/agent-output/

# IDEs
.idea/
*.iml
*.ipr
*.iws
.vscode/
.project
.classpath
.settings/
.factorypath
nbproject/private/
nbbuild/
nbdist/
.nb-gradle/
.eclipse/
bin/

# Logs
*.log
logs/
hs_err_pid*
replay_pid*

# OS
.DS_Store
Thumbs.db
desktop.ini
$RECYCLE.BIN/

# Env / local config
.env
.env.local
*.local
